<% @page_title = "会場編集" %>
<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "会場情報に戻る", admin_place_path(@place) %></div>

<%= form_for @place, as: "place", url: admin_place_path(@place) do |form| %>
    <%= render "place_form", form: form %>
<% end %>


